GPU 拓扑感知调度器

适用方向：AI Infra 研发专家（GPU 调度与网络优化方向）

题目：主题 / 场景设定

设计并实现一个面向 GPU 集群的**拓扑感知调度器。

假设集群中有多台 GPU 服务器，每台服务器包含若干 GPU。用户会不断提交训练任务，每个任务需要申请一定数量的 GPU。

例如：

- Node A：8 GPU
- Node B：8 GPU
- Node C：8 GPU
- Node D：8 GPU

任务：

- Job A：需要 8 GPU
- Job B：需要 4 GPU
- Job C：需要 8 GPU

对于同一个任务，GPU 尽量分配在更少的节点上，可以减少跨节点通信开销；但过度追求集中分配，也可能产生 GPU 资源碎片。

请设计一个调度器，在**资源利用率、资源碎片和跨节点通信成本**之间进行权衡。

推荐产品形态：

Scheduler Simulator + CLI 或简单 Web 页面

不要求真实 Kubernetes 或 GPU 环境，可通过模拟数据完成。

必须支持

- 支持定义 Node 和 GPU 资源
- 支持提交、运行、结束多个 GPU 任务
- 至少实现两种调度策略：
  - 基础策略，例如 First Fit / Random
  - 自己设计的 Topology-aware 策略
- 优先减少单个任务跨 Node 使用 GPU 的数量
- 能够处理 GPU 资源不足和资源碎片场景
- 展示每个任务最终分配到了哪些 Node / GPU
- 展示基本的调度原因，例如：
  - 为什么选择这些节点
  - 为什么任务暂时无法调度
- 至少统计以下指标：
  - GPU 利用率
  - 等待任务数量或平均等待时间
  - 跨 Node GPU 任务数量
- 使用同一组任务，对比两种调度策略的运行效果



加分能力

以下能力不作为基础完成要求：

- 支持任务优先级
- 支持任务抢占
- 支持多租户 Queue / Quota
- 将 NVLink / NVSwitch 等 GPU 内部拓扑纳入调度
- 根据 DP / TP / PP 等并行方式设计不同放置策略
- 实现 Kubernetes Scheduler Plugin 或 Volcano Plugin
- 在真实 GPU 集群中进行验证



候选人提交要求

- 可运行 Demo 及完整源代码
- 至少提供 2 种 GPU 集群配置
- 至少提供 20 个模拟任务
- 调度策略说明
- 两种调度策略的效果对比
- 基础功能及异常场景测试
- README，包括运行方式、核心设计和已知限制



评分标准

- GPU 资源及调度模型设计：20 分
- Topology-aware 调度策略：25 分
- 资源碎片及通信成本处理：20 分
- 调度结果展示与指标分析：15 分
- 异常场景及程序稳定性：10 分
- 代码质量、测试及文档：10 分